# Results

- `metrics/`: Machine-readable and presentation-ready metric tables.
- `plots/`: Trajectory, speed, error and uncertainty figures.
- `trajectories/`: Timestamped replay outputs for each evaluated method.

Each result should record its configuration, dataset trip and Git commit.

