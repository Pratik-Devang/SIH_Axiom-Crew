<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ── Corner Radii (Premium, generous rounding) ──────────────────────── -->
    <dimen name="radius_xs">6dp</dimen>
    <dimen name="radius_sm">12dp</dimen>
    <dimen name="radius_md">16dp</dimen>
    <dimen name="radius_lg">24dp</dimen>
    <dimen name="radius_xl">32dp</dimen>
    <dimen name="radius_full">999dp</dimen>

    <!-- ── Spacing (Generous, breathable) ──────────────────────────────────── -->
    <dimen name="space_xs">4dp</dimen>
    <dimen name="space_sm">8dp</dimen>
    <dimen name="space_md">16dp</dimen>
    <dimen name="space_lg">24dp</dimen>
    <dimen name="space_xl">32dp</dimen>
    <dimen name="space_xxl">48dp</dimen>

    <!-- ── Touch Targets ───────────────────────────────────────────────────── -->
    <dimen name="touch_min">48dp</dimen>
    <dimen name="touch_lg">56dp</dimen>
    <dimen name="touch_xl">64dp</dimen>

    <!-- ── Card / Sheet Elevation ────────────────────────────────────────── -->
    <dimen name="card_padding">20dp</dimen>
    <dimen name="card_padding_sm">16dp</dimen>
    <dimen name="sheet_padding_h">24dp</dimen>
    <dimen name="elevation_card">8dp</dimen>
    <dimen name="elevation_sheet">16dp</dimen>
    <dimen name="elevation_fab">12dp</dimen>
    <dimen name="elevation_search">16dp</dimen>

    <!-- ── Floating Map Controls ───────────────────────────────────────────── -->
    <dimen name="fab_size">56dp</dimen>
    <dimen name="fab_size_lg">64dp</dimen>
    <dimen name="fab_margin">16dp</dimen>

    <!-- ── Maneuver Card ──────────────────────────────────────────────────── -->
    <dimen name="maneuver_icon_size">64dp</dimen>
    <dimen name="maneuver_distance_text">42sp</dimen>
    <dimen name="maneuver_instruction_text">18sp</dimen>

    <!-- ── Typography (Modern, readable hierarchy) ─────────────────────────── -->
    <dimen name="text_h1">36sp</dimen>
    <dimen name="text_h2">28sp</dimen>
    <dimen name="text_h3">22sp</dimen>
    <dimen name="text_h4">18sp</dimen>
    <dimen name="text_body">16sp</dimen>
    <dimen name="text_body_sm">14sp</dimen>
    <dimen name="text_caption">13sp</dimen>
    <dimen name="text_micro">11sp</dimen>

    <!-- ── Bottom Sheet ───────────────────────────────────────────────────── -->
    <dimen name="sheet_handle_width">40dp</dimen>
    <dimen name="sheet_handle_height">4dp</dimen>
    <dimen name="sheet_top_radius">28dp</dimen>

    <!-- ── Search Bar ─────────────────────────────────────────────────────── -->
    <dimen name="search_bar_height">56dp</dimen>
    <dimen name="search_margin_top">40dp</dimen>
    <dimen name="search_margin_horizontal">20dp</dimen>

    <!-- ── Status Chips ───────────────────────────────────────────────────── -->
    <dimen name="chip_height">32dp</dimen>
    <dimen name="chip_padding_horizontal">12dp</dimen>
    <dimen name="chip_radius">16dp</dimen>

    <!-- ── Navigation Bar ────────────────────────────────────────────────── -->
    <dimen name="nav_bar_height">80dp</dimen>
    <dimen name="nav_bar_icon_size">28dp</dimen>
</resources>
